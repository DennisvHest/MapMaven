@using Squirrel

Update (@Update?.FutureReleaseEntry?.Version) has been downloaded. Restart to apply the update. <MudLink OnClick="UpdateReleaseNotes" Color="Color.Info" Underline="Underline.Always">What has changed</MudLink>

@code {
    [Inject]
    IDialogService DialogService { get; set; }

    [Parameter]
    public UpdateInfo Update { get; set; }

    void UpdateReleaseNotes()
    {
        DialogService.Show<ReleaseNotes>(
            title: $"Map Maven {Update?.FutureReleaseEntry?.Version}",
            parameters: new(),
            options: new DialogOptions
            {
                CloseButton = true
            }
        );
    }
}
