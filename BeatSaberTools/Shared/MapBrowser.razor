@inherits ReactiveComponent

@using BeatSaberTools.Core.Utilities.Scoresaber

<MudTable Items="Maps"
          Height="calc(100% - 116px)"
          Class="full-height background-transparent"
          Loading="LoadingMapInfo"
          Filter="Filter"
          FixedHeader="true"
          RowStyle="height: 88px">
    <ToolBarContent>
        @if (SelectedPlaylist != null)
        {
            <MudChip Variant="Variant.Outlined">@SelectedPlaylist.Title</MudChip>
        }
        @if (SelectedSongAuthorName != null)
        {
            <MudChip Variant="Variant.Outlined" OnClose="RemoveSongAuthorFilter">@SelectedSongAuthorName</MudChip>
        }
        <MudTextField @bind-Value="SearchString"
                      DebounceInterval="300"
                      Immediate="true"
                      Placeholder="Search"
                      Variant="Variant.Outlined"
                      Adornment="Adornment.Start"
                      AdornmentIcon="@Icons.Material.Filled.Search"
                      IconSize="Size.Small"
                      Class="mt-0">
        </MudTextField>
    </ToolBarContent>
    <HeaderContent>
        @HeaderContent
    </HeaderContent>
    <RowTemplate>
        @if (RowContent != null)
        {
            @RowContent(context)
        }
    </RowTemplate>
    <PagerContent>
        <MudTablePager />
    </PagerContent>
</MudTable>