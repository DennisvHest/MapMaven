<MudDrawer Open="true" Elevation="1">
    <MudNavLink
        Icon="@Icons.Filled.LibraryMusic"
        DisableRipple="true"
        OnClick="@(() => OnMapsNavigation("/"))">
        Maps
    </MudNavLink>
    <MudNavLink
        Icon="@Icons.Filled.LibraryMusic"
        DisableRipple="true"
        OnClick="@(() => OnMapsNavigation("/ranked"))">
        Ranked
    </MudNavLink>
    <PlaylistList></PlaylistList>
</MudDrawer>
